Drop your two "Add a friend" example screenshots here so they show on the
registration page:

  add-friend-id.png   – the "ADD A FRIEND" dialog that shows "Your player ID"
  add-friend-qr.png   – the profile / QR screen with "SHARE LINK" and the QR code

They are referenced from the registration form as /help/add-friend-id.png and
/help/add-friend-qr.png. If a file is missing, its slot is hidden automatically,
so nothing breaks until you add them. Recommended width ~600px, PNG or JPG.
